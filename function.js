//function is a reusable block of code
function addTwoNumber() {
  let results = 7 + 9;
  console.log(results);
}
addTwoNumber(); //invoking a function
//example1
function addTwoNumber(num1, num2) {
  let results = num1 + num2;
  return results;
}
console.log(addTwoNumber(7, 90));
let newResults = addTwoNumber(45, 60);
console.log(newResults);
//example2
function checkLengthOfString(str) {
  return str.length;
}
console.log(checkLengthOfString("albert"));

/*
//write a function to print all even no greater than 30 but less than 50
function evenNumbers(x,y){
let m;
for (let m=31; m < 50; m++) {
  if (m % 2 == 0) 
  }
  console.log(m);
}
evenNumbers()
 * 
 */
/*
function evenNumbers(lm, ul) {
  let runningNumb = lm + 1;
  for (runningNumb; runningNumb < ul; runningNumb++) {
    if (runningNumb % 2 == 0) {
      console.log(runningNumb);
    }
  }
}
evenNumbers(30, 50);


*/
// write a function that takes an array of numbers as an argument and returns
//the sum of the numbers in the array.then call the funtion

function addition(arr nums) {
  let sum =arr + 1;
  for (sum<nums.lenght sum+1){
    sum += num[sum];
  }
  return sum;
  } 
  // average = sum / nums.length;
  console.log(average);
  return sum;
sum(94, 9, 16, 25);

//write that takes a string as an argument and returns a string with all vowels removed
//write a  function that takes an array of numbers as an argument and returns the averageof those numbers.

let kansoul = {
  members: ["mejja", "madtraxx", "jjhh", ""],
  album: 5,
  sing: function (title) {
    console.log("title");
    //DOM manipulation
  },
};
console.log(kansoul.sing); //5

